package id.co.bankmandiri.di.module

import org.koin.dsl.module

val repositoryModule = module {
    // TODO if needed
    // single { AllMovieRepository(get(), get()) }
    // single { MovieRepository(get()) }
    // single { TvRepository(get()) }
}