package id.co.bankmandiri.di.module

import org.koin.dsl.module

val useCaseModule = module {
    // TODO if needed
    // single { AllMovieUseCase(get()) }
    // single { MovieUseCase(get()) }
    // single { TvUseCase(get()) }
}